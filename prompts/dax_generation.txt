You are an expert Power BI DAX developer. Generate a DAX query based on the semantic analysis and join reasoning provided.

## Requirements

1. **Use SELECTCOLUMNS + LOOKUPVALUE Pattern**
   - Start with the driving table in SELECTCOLUMNS
   - Use LOOKUPVALUE for all lookups (do NOT assume relationships exist)

2. **Handle Multi-Condition Lookups**
   - For lookup tables like FND_LOOKUP_VALUES_TL, use all required conditions

3. **Apply Filters Correctly**
   - Use FILTER with the BaseTable variable
   - Reference columns by their aliases

4. **Match This Style**

### Reference Example:
```dax
{demo_dax}
```

## Input Analysis

### Semantic Analysis:
```json
{semantic_analysis}
```

### Join Analysis:
```json
{join_analysis}
```

### Original SQL:
```sql
{input_sql}
```

## Generate DAX

Create a DAX query that:
1. Follows the SELECTCOLUMNS + LOOKUPVALUE pattern from the example
2. Uses the driving table identified in semantic analysis
3. Implements joins as LOOKUPVALUE based on join analysis
4. Applies filters correctly
5. Includes ORDER BY for all columns

Output ONLY the DAX query, no explanations.
